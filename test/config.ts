// These are being hardcoded for the test.
// Obtain these from https://dashboard.finverse.com
export const config = {
  apiHost: 'https://demo-api.dev.finverse.net',
  redirectURI: 'https://demo-api.dev.finverse.net/callback',
  clientId: 'clientId',
  clientSecret: 'clientSecret',
};
